Put a labeled dataset here named `labeled_dataset.csv` before running `train_model.py`.

Required columns: `temperature,current,vibration,rpm,label`

This directory is intentionally empty otherwise - no synthetic/fake training
data is included, per the project's "no fake ML accuracy" requirement.
